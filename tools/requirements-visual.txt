# Optional dependencies for tools/visual_structure_extract.py
# Stable path: PaddleOCR 2.x + PP-Structure 2.x.
# PaddleOCR 3.x changed public APIs and removed the old PPStructure entry point,
# so this tool intentionally pins <3 until a dedicated 3.x adapter is added.

paddleocr>=2.7.0,<3.0.0
paddlepaddle>=2.6.0,<3.0.0

# Server-friendly OpenCV build for overlays and flowchart candidate detection.
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0,<11.0.0
